[package]
name = "vk-origin-replay"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = "0.8.4"
tokio = { version = "1", features = ["full"] }
tower-http = { version = "0.5.2", features = ["validate-request"] }
url = "2.5.8"
relay-client = { path = "relay_client_shim" }

# standalone; never attach to another workspace
[workspace]
